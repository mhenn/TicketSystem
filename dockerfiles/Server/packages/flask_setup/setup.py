from setuptools import setup

setup(name='flask_setup',
            version='0.1',
            install_requires=[
                'flask',
                'flask_restx',
                'flask_cors',
                'flask_jwt_extended',
                'requests'
                ])
